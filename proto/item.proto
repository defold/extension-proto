syntax = "proto2";

package Game;

message Item {
  enum ItemType {
    WEAPON = 0;
    ARMOUR = 1;
    RING   = 2;
    BOOK   = 3;
  }
  required int32    id      = 1;
  required string   name    = 2;
  required float    weight  = 3;
  required ItemType type    = 4;
}
